We can use the *.log in the .gitignore file to ignore all the log files
in the project. we can also use *.ext method to ignore any files and it's
extension.

I am going to check how git diff works in this file for staged and unmodified files.